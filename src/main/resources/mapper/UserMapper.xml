<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lolreview.summonerreview.repository.UserMapper">
    <select id="findByUsername" parameterType="string" resultType="com.lolreview.summonerreview.entity.User">
        SELECT * FROM users WHERE username = #{0}
    </select>

    <insert id="insertUser" parameterType="com.lolreview.summonerreview.entity.User">
        INSERT INTO users (username, password, email, role)
        VALUES (#{username}, #{password}, #{email}, #{role})
    </insert>
</mapper> 